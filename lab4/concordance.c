#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main (int argc, char *argv[])
{
    if(argc < 2) {
        printf("Usage: %s <str1> <str2>\n", argv[0]);
        exit(EXIT_FAILURE);
    }
    else {

        exit(EXIT_SUCCESS);
    }
    return 0;
}